---
driver:
  name: vagrant

provisioner:
  name: chef_zero

platforms:
  - name: ubuntu-12.04

suites:
  - name: default
    run_list:
      - recipe[pgtest::master]
      - recipe[pgtest::create_user]
      - recipe[pgtest::create_database]
    attributes:
